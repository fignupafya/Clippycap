"""Clippycap -- gameplay clip library + annotation tool."""

# Keep this in sync with pyproject.toml's `version` and packaging/installer.iss's `AppVersion`.
# The update check compares the GitHub Releases latest tag to this string.
__version__ = "0.4.1"
